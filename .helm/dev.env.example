# Modelo das variáveis de ambiente de DEV do backend.
# Copie para .helm/backend.env (git-ignored) e preencha.
# Usado pelo run config do IntelliJ (.run/Backend UP.run.xml -> envFilePaths).
APROOVE_DB_PASSWORD=dev-local
APROOVE_OIDC_CLIENT_SECRET=<client-secret-do-client-LOCAL-roove-no-SSO>
