<!--
    Limpeza pós-teste do ExameResourceTest. O DbUnit não participa da transação do
    teste (abre conexão própria, em autocommit), então o que o @DatabaseSetup insere
    só sai daqui — sem isso as linhas sobram e o CLEAN_INSERT da execução seguinte
    esbarra nas FKs.

    DELETE_ALL apaga as tabelas na ordem inversa da que aparecem aqui, por isso a
    sequência é a mesma do dataset de carga: pai primeiro, filho por último.
-->
<dataset>
    <paciente/>
    <upload/>
    <exame/>
</dataset>
